# Manzil — pinned dependencies (Phase 0 baseline)

# Core
pydantic==2.9.2
python-dotenv==1.0.1

# LLM
google-generativeai==0.8.3

# HTTP
httpx==0.27.2

# UI
streamlit==1.39.0
folium==0.17.0
streamlit-folium==0.23.1

# Vector store / RAG (used from Phase 3 — DEFERRED to Phase 3 install).
# chromadb pulls onnxruntime which on some Windows / msys2 Python builds fails
# with an SSL cert error during the cmake bootstrap. We'll install it at the
# start of Phase 3 once we actually need it.
chromadb==1.5.8

# Agent graph (used from Phase 1 — installed early)
langgraph==0.2.39

# Data
numpy==1.26.4
pandas==2.2.3

# Testing
pytest==8.3.3
pytest-mock==3.14.0
