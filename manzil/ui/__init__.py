"""Manzil Streamlit UI (legacy).

The primary UI is the Next.js frontend in ``frontend/``. This Streamlit app is
kept as a fallback / demo harness and lives alongside the Python package it
depends on. Run from the project root::

    streamlit run manzil/ui/app.py
"""
