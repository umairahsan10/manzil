# Manzil — FastAPI backend dependencies

# Core (must match root requirements)
pydantic==2.9.2
python-dotenv==1.0.1

# FastAPI server
fastapi==0.115.0
uvicorn[standard]==0.30.6

# HTTP
httpx==0.28.1

# Phase 2 database (installed now, used later)
sqlmodel==0.0.22
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3

# CORS
python-multipart==0.0.9
